# CLAUDE.md

## 项目：家庭迎新生命操作手册

本文件用于约束 Claude Code 在本仓库中的行为。

---

## 1. 你的工作方式

将本项目视为“文档工程项目”，而不是普通写作任务。

你的主要角色是：

- 文档架构师
- Markdown 内容工程师
- 信息架构审查员
- 医学安全辅助审查员
- Git 友好的增量修改执行者

优先考虑：

1. 结构稳定
2. 内容准确
3. 易于维护
4. 方便打印
5. 可持续迭代

---

## 2. 开始任务前必须阅读

每次开始工作前，按以下顺序检查：

1. `README.md`
2. `AGENTS.md`
3. `docs/00-project/project-overview.md`
4. `docs/00-project/writing-standard.md`
5. `docs/00-project/module-overview.md`
6. 当前目标模块的 `README.md`
7. 本次需要修改的章节

若规范之间冲突，优先级为：

```text
用户当前明确指令
> AGENTS.md
> CLAUDE.md
> docs/00-project/writing-standard.md
> 模块 README.md
> 章节现有约定
```

发现冲突时，应明确指出，不要自行猜测。

---

## 3. 当前项目状态

当前已完成：

- 根目录入口
- 项目管理文档
- 八个业务模块
- 月子 42 天逐日空模板
- 宝宝 0–1 岁逐月空模板
- 通用记录模板
- 资源与发布目录

当前尚未完成：

- 第一卷至第六卷的具体章节文件
- 医学信息来源与核验规范
- 业务正文
- PDF/Word 构建流程

不要误认为项目已经进入大规模正文生成阶段。

---

## 4. 修改策略

### 4.1 默认采用增量修改

一次任务优先只做：

- 一个模块的章节设计
- 一个章节正文
- 一组高度相关的 2–3 个文件
- 一类格式或导航修复

### 4.2 禁止大范围自由发挥

除非用户明确要求，否则不要：

- 一次生成全部八卷正文
- 自动创建大量新章节
- 改变目录结构
- 重写全部 README
- 将 day 文件合并
- 将 month 文件合并
- 删除记录模板
- 引入复杂构建工具

### 4.3 先展示方案的操作

以下操作先给出方案，再执行：

- 第一卷至第六卷章节拆分
- 批量新建超过 10 个文件
- 目录或命名规则调整
- 发布流程设计
- 医学引用规范调整
- 模板结构调整

---

## 5. 文件命名

- 文件名使用英文小写
- 单词之间使用短横线
- day 文件保持两位数字
- month 文件保持两位数字
- README.md 保持大写
- 不使用空格、中文文件名和随机缩写

示例：

```text
prenatal-checkup.md
hospital-admission.md
cesarean-recovery.md
day-01.md
month-01.md
```

---

## 6. 中文写作风格

正文要求：

- 简体中文
- 清楚、直接、克制
- 以操作步骤为主
- 每段尽量简短
- 避免重复解释
- 专业词首次出现时解释
- 明确角色与责任人
- 对时间节点使用具体表达

推荐：

> 破水后即使没有宫缩，也应按照建档医院的要求及时联系产科并前往医院。

避免：

> 破水是一个非常重要、非常危险、所有人都必须高度重视的问题。

---

## 7. 章节结构

除非模块规范另有说明，业务章节使用：

```markdown
# 标题

## 本章目标

## 为什么重要

## 时间节点

## 操作流程

## 注意事项

## 常见错误

## 医疗风险提醒

## Checklist

- [ ]

## 记录区域
```

写正文时应把空标题填充完整，不保留无意义占位内容。

---

## 8. 医学内容处理

### 8.1 写作边界

本项目可以提供：

- 一般健康教育
- 日常护理方法
- 就医风险提示
- 与医生沟通的问题清单
- 记录模板

本项目不能替代：

- 诊断
- 处方
- 医生面诊
- 医院具体制度
- 个体化治疗方案

### 8.2 遇到不确定信息

使用以下之一：

- `> 需与建档医院确认`
- `> 需由产科医生评估`
- `> 不同医院流程可能不同`
- `> 本节仅用于家庭观察，不替代诊疗`

不得用看似确定的语气填补未知内容。

### 8.3 紧急风险内容

危险信号应：

- 单独成段
- 使用清晰标题
- 给出动作优先级
- 避免使用模糊表达
- 不让家庭护理延误急诊

---

## 9. README 维护

新增章节后，同步检查：

- 当前模块 README 的章节导航
- 根 README 的全卷导航
- 相关跨模块链接
- 文件名与标题一致性

不得创建孤立文件。

---

## 10. Mermaid 使用

Mermaid 适用于：

- 临产判断
- 入院流程
- 顺产/剖宫产路径
- 夜间分工
- 宝宝异常处理升级路径

不适用于：

- 装饰性图表
- 纯知识罗列
- 复杂医学诊断树

生成后检查：

- fenced code block 闭合
- 节点 ID 不重复
- 中文标点不破坏语法
- 链路方向合理

---

## 11. 模板复用

优先复用：

- `templates/daily-checklist.md`
- `templates/hospital-record.md`
- `templates/mother-record.md`
- `templates/baby-record.md`

不要在多个章节重复创建功能完全相同的记录表。

如需新增模板，先说明现有模板为何不足。

---

## 12. 验证要求

修改完成后至少检查：

- Markdown 标题层级
- 相对链接
- 文件命名
- Checklist 格式
- Mermaid 代码块
- 重复内容
- 是否超出任务范围
- 是否包含未经核验的医学结论

如仓库存在 lint 或测试命令，优先执行。

---

## 13. 任务汇报格式

完成任务后使用以下结构：

```markdown
## 已完成

- ...

## 修改文件

- `path/to/file.md`

## 关键说明

- ...

## 需要人工确认

- ...

## 未处理

- ...

## 下一步建议

- ...
```

---

## 14. 禁止操作

未经用户明确要求，不得：

- 自动提交 Git
- 自动推送远程仓库
- 创建或合并 Pull Request
- 删除文件
- 修改用户个人记录
- 引入未经批准的依赖
- 生成最终 PDF/Word
- 批量覆盖现有内容

---

## Prompt 与文件变更追踪规范

本仓库采用 Prompt 驱动开发模式。

所有 AI 生成、修改、删除文件的行为，都必须能够追溯到具体 Prompt。

## Prompt 历史记录文件

统一维护：

    docs/00-project/prompt-history.md

## 记录内容

需要记录：

-   Prompt 文件名称
-   Prompt 版本
-   执行 Agent
-   执行时间
-   操作范围
-   创建文件
-   修改文件
-   删除文件
-   Review 状态

## AI 执行要求

以下操作完成后必须更新 prompt-history：

-   初始化目录
-   新增章节文件
-   批量生成 Markdown
-   修改模板
-   修改项目规范
-   生成发布文件

## 推荐格式

``` markdown
## 日期

### Prompt

Prompt文件名称

### Agent

Codex / Claude Code

### 操作

描述执行目标

### 文件变化

新增：
- file.md

修改：
- file.md

删除：
- file.md

### Review

待审核 / 已审核
```

## 禁止

未经记录：

-   不执行大规模文件生成
-   不覆盖大量已有内容
-   不调整项目结构
---

## 15. 推荐的下一步

当前最合理的下一阶段是：

1. 为 `docs/01-late-pregnancy/` 设计细粒度章节目录
2. 评审章节边界和英文文件名
3. 仅创建空骨架
4. 人工确认后再逐章编写内容

不要直接开始生成完整第一卷正文。

---

## 16. AI 文档工程治理规则

### 16.1 新终端恢复规则

新终端或上下文丢失后，不根据目录名称自行推断任务。必须先按 `docs/00-project/project-context.md` 中的“上下文恢复顺序”读取治理文件，再确认当前阶段和任务范围。

### 16.2 修改范围控制

- 将当前用户指令和对应 Prompt 作为唯一任务边界。
- 不重新初始化已有项目。
- 不批量覆盖已有文件；目标存在时先读取并增量合并。
- 不修改未授权的业务模块、模板、个人记录或发布产物。
- Prompt 存放在 `prompts/`，执行结果存放在 `docs/`，治理信息存放在 `docs/00-project/`。

### 16.3 任务闭环

Prompt 执行完成后必须：

1. 更新 `docs/00-project/prompt-history.md`，记录具体创建、修改、删除和迁移文件。
2. 在阶段切换或重大任务完成后更新 `docs/00-project/project-context.md`。
3. 输出逐项文件清单，不能只汇报数量。
4. 输出风险与待人工确认项。
5. 只提出与当前阶段一致的下一步建议。

未经用户明确授权，不执行 Git commit、Git push、PR 创建或合并。

---

## 17. Frozen 工程结构保护

开始任务前检查根目录 `STATUS.md`。如果其中显示：

```text
Phase Status = Frozen
```

不得主动修改目录层级、治理体系或 Prompt 生命周期。

冻结后只允许新增 Prompt、新增内容和修复 Bug。除非用户明确要求，否则不能以优化、重构或统一格式为理由改变工程结构。

用户明确要求修改冻结结构时，执行前必须说明影响范围，并新增或更新 ADR。
